# 🚀 Oracle Cloud 무료 티어 배포 가이드

## 📋 Oracle Cloud 무료 제공 (영구 무료!)

| 서비스 | 제공량 | 용도 |
|--------|--------|------|
| **Compute VM** | 2개 (각 1GB RAM) | API + Worker |
| **Autonomous DB** | 20GB | PostgreSQL 대체 |
| **Block Volume** | 200GB | 데이터 저장 |
| **Object Storage** | 10GB | 정적 파일 |
| **Load Balancer** | 1개 (10Mbps) | 트래픽 분산 |
| **Outbound Data** | 10TB/월 | API 트래픽 |

---

## 🔧 Step 1: Oracle Cloud 계정 생성

1. https://www.oracle.com/cloud/free/ 접속
2. **Start for free** 클릭
3. 정보 입력 (신용카드 필요하지만 무료 티어는 과금 안됨)
4. 리전 선택: **South Korea Central (Seoul)** 또는 가까운 곳

---

## 🖥️ Step 2: Compute VM 생성

### VM 1: API 서버

1. **Compute > Instances > Create Instance**
2. 설정:
   - Name: `pricedrop-api`
   - Image: **Oracle Linux 8** 또는 **Ubuntu 22.04**
   - Shape: **VM.Standard.E2.1.Micro** (무료!)
   - OCPU: 1, Memory: 1GB
3. SSH 키 다운로드 (중요!)
4. **Create** 클릭

### VM 2: Database 서버

1. 같은 방법으로 두 번째 VM 생성
   - Name: `pricedrop-db`
   - 나머지 동일

---

## 🔐 Step 3: 보안 규칙 설정

1. **Networking > Virtual Cloud Networks**
2. VCN 선택 > **Security Lists** > **Default Security List**
3. **Add Ingress Rules**:

| Port | Protocol | 용도 |
|------|----------|------|
| 22 | TCP | SSH |
| 80 | TCP | HTTP |
| 443 | TCP | HTTPS |
| 8000 | TCP | API |
| 5432 | TCP | PostgreSQL (내부만) |
| 6379 | TCP | Redis (내부만) |

---

## 📦 Step 4: 서버 설정 스크립트

### VM 1 (API) 접속 및 설정:

```bash
# SSH 접속
ssh -i ~/oracle-key.pem opc@<VM1-PUBLIC-IP>

# 설정 스크립트 실행
curl -fsSL https://raw.githubusercontent.com/yourusername/pricedrop/main/deploy/oracle-cloud/setup-api.sh | bash
```

### VM 2 (DB) 설정:

```bash
ssh -i ~/oracle-key.pem opc@<VM2-PUBLIC-IP>

curl -fsSL https://raw.githubusercontent.com/yourusername/pricedrop/main/deploy/oracle-cloud/setup-db.sh | bash
```

---

## 🌐 Step 5: 도메인 연결 (선택사항)

1. **무료 도메인**: https://freenom.com 에서 무료 도메인 발급
2. A 레코드 설정:
   - `api.yourdomain.tk` → VM1 IP
3. Let's Encrypt SSL 무료 인증서:
   ```bash
   sudo certbot --nginx -d api.yourdomain.tk
   ```

---

## ✅ 최종 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                 Oracle Cloud (무료 티어)                 │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   ┌─────────────────┐      ┌─────────────────┐         │
│   │   VM 1 (API)    │      │   VM 2 (DB)     │         │
│   │  1GB RAM        │      │  1GB RAM        │         │
│   ├─────────────────┤      ├─────────────────┤         │
│   │ • Nginx         │      │ • PostgreSQL    │         │
│   │ • FastAPI       │◄────►│ • Redis         │         │
│   │ • Celery Worker │      │                 │         │
│   │ • Celery Beat   │      │                 │         │
│   └────────┬────────┘      └─────────────────┘         │
│            │                                            │
│            ▼                                            │
│   ┌─────────────────┐                                  │
│   │ Load Balancer   │ ◄── 무료 SSL                     │
│   │ (10 Mbps 무료)  │                                  │
│   └────────┬────────┘                                  │
│            │                                            │
└────────────┼────────────────────────────────────────────┘
             │
             ▼
        인터넷 (사용자)
```

---

## 💰 비용 요약

| 항목 | 월 비용 |
|------|--------|
| VM 2개 | **$0** |
| Storage 200GB | **$0** |
| Load Balancer | **$0** |
| Outbound 10TB | **$0** |
| **총합** | **$0/월** |

---

## ⚠️ 주의사항

1. **Always Free** 리소스만 사용할 것
2. Shape 선택 시 **Always Free Eligible** 확인
3. 무료 티어 초과 시 자동 과금 방지: Budget Alert 설정
4. 신용카드는 인증용이며 무료 티어는 과금 안됨
